# 🤖 WB Fines Monitor Bot
Автоматизированная система мониторинга штрафов Wildberries. Бот отслеживает 
новые финансовые санкции в личном кабинете поставщика WB и отправляет 
мгновенные уведомления в Telegram.

![Python](https://img.shields.io/badge/Python-3.8%2B-yellow)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-13+-blue)

## 🚀 Функциональность:
Основные возможности
Автоматический мониторинг штрафов Wildberries 24/7
Мгновенные уведомления в Telegram о новых штрафах
Сохранение истории всех штрафов в PostgreSQL
Гибкая конфигурация через переменные окружения
Два режима работы: MOCK (тестовый) и PROD (боевой)

Детали реализации
Интервал проверки: Настраивается (по умолчанию 5 минут)
Пороги уведомлений: Настройка для крупных штрафов
Логирование: Подробные логи в файл и консоль
Обработка ошибок: Автоматический перезапуск при сбоях

Технологический стек
Язык: Python 3.11+
Фреймворки: FastAPI (мок-сервер), SQLAlchemy (ORM)
База данных: PostgreSQL 15+
Уведомления: Telegram Bot API
Контейнеризация: Docker, Docker Compose
Логирование: стандартный logging модуль Python

## ⚙️ Установка:
Предварительные требования
Python 3.11 или выше
PostgreSQL 15+ (или Docker)
Аккаунт Telegram для создания бота
API доступ к Wildberries (для PROD режима)

Пошаговая установка
1. Клонирование репозитория:
git clone <репозиторий>
cd wb_bot

2. Создание виртуального окружения:
python -m venv .venv
source .venv/bin/activate  # Linux/Mac

.venv\Scripts\activate     # Windows

3. Установка зависимостей:
pip install -r requirements.txt

4. Запуск PostgreSQL через Docker:
docker-compose up -d

5. Настройка конфигурации:
cp .env.example .env

### Получение необходимых ключей
Telegram Bot Token
Откройте Telegram
Найдите @BotFather
Выполните /newbot
Сохраните полученный токен

Telegram Chat ID
Найдите @userinfobot в Telegram
Отправьте любое сообщение
Сохраните ваш chat_id
Wildberries API Key (для PROD режима)
Войдите в личный кабинет поставщика WB
Настройки → Доступ к API → Создать новый токен
Выберите права на чтение штрафов

## 🚀 Использование
### Запуск в тестовом режиме (MOCK)
Запустите мок-сервер:
cd mock_server
python main.py

Запустите бота:
python bot/main.py

### Запуск в боевом режиме (PROD)
Обновите .env файл:
APP_MODE=PROD
WB_API_KEY=ваш_реальный_api_ключ

Запустите бота:
python bot/main.py

## 🌐 API Эндпоинты:
Мок-сервер (тестовый режим):
GET  /health              Проверка здоровья сервера
GET  /api/v3/fines        Получение списка штрафов
     Параметры: days (за сколько дней)

Реальный API Wildberries (PROD режим):
GET  /api/v3/fines        Получение штрафов
     Параметры: dateFrom (дата в формате ISO)
GET  /api/v1/info         Информация о продавце
POST /api/v3/fines/{id}/dispute  Оспаривание штрафа

## 🗃️ Структура базы данных:
Таблица fines:
CREATE TABLE fines (
    id VARCHAR(50) PRIMARY KEY,        -- Уникальный ID штрафа
    date TIMESTAMP NOT NULL,           -- Дата начисления
    type VARCHAR(200) NOT NULL,        -- Тип нарушения
    amount DECIMAL(10, 2) NOT NULL,    -- Сумма штрафа
    order_id VARCHAR(50),              -- Номер заказа
    status VARCHAR(50),                -- Статус (Начислен/Оспорен/Оплачен)
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,  -- Дата создания записи
    notified BOOLEAN DEFAULT FALSE     -- Флаг отправки уведомления
);

Таблица notifications:
CREATE TABLE notifications (
    id SERIAL PRIMARY KEY,             -- Уникальный ID уведомления
    fine_id VARCHAR(50),               -- Ссылка на штраф
    channel VARCHAR(50) NOT NULL,      -- Канал отправки (telegram)
    sent_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,  -- Время отправки
    success BOOLEAN DEFAULT TRUE,      -- Успешность отправки
    error_message TEXT                 -- Текст ошибки (если была)
);

## 🔍 Процессы работы бота:
1. Инициализация:
# Последовательность инициализации:
1. Загрузка конфигурации из .env
2. Проверка валидности настроек
3. Подключение к PostgreSQL
4. Создание таблиц (если не существуют)
5. Проверка подключения к Telegram
6. Проверка доступности WB API

2. Цикл мониторинга:
while True:
    # 1. Получение штрафов за последние 24 часа
    fines = wb_api.get_fines(date_from=last_check)
    
    # 2. Фильтрация новых штрафов
    new_fines = filter_new_fines(fines)
    
    # 3. Сохранение в БД
    for fine in new_fines:
        save_to_database(fine)
        
        # 4. Отправка уведомления
        if fine.amount > HIGH_FINE_THRESHOLD:
            send_urgent_notification(fine)
        else:
            send_regular_notification(fine)
        
        # 5. Отметка как уведомленного
        mark_as_notified(fine.id)
    
    # 6. Пауза до следующей проверки
    sleep(CHECK_INTERVAL)

3. Обработка ошибок
Ошибка API: повторная попытка через 1 минуту
Ошибка БД: логирование и продолжение работы
Ошибка Telegram: попытка отправить упрощенное сообщение
Критическая ошибка: остановка бота с уведомлением

## 🛠️ Разработка
Структура проекта:
wb_bot/
├── bot/                    # Основной код бота
│   ├── __init__.py
│   ├── config.py          # Конфигурация
│   ├── wb_client.py       # Клиент WB API
│   ├── notifications.py   # Отправка уведомлений
│   └── main.py           # Главный файл бота
├── database/              # Работа с БД
│   ├── __init__.py
│   ├── models.py         # Модели SQLAlchemy
│   └── repository.py     # Репозитории для работы с данными
├── mock_server/          # Мок-сервер для тестов
│   └── main.py
├── tests/                # Тесты
├── docker-compose.yml    # Конфигурация Docker
├── requirements.txt      # Зависимости Python
├── .env                  # Конфигурация (не в git)
├── .env.example         # Пример конфигурации
└── README.md            # Эта документация

## 📊 Мониторинг и логирование:
Файлы логов
bot.log - Основные логи работы бота
bot_final.log - Логи упрощенной версии
Docker logs - Логи PostgreSQL контейнера

Ключевые метрики для мониторинга
Количество проверок в день
Количество найденных штрафов
Время отклика API
Успешность отправки уведомлений
Размер базы данных

Пример запроса для мониторинга:
-- Статистика за сегодня
SELECT 
    COUNT(*) as total_fines,
    SUM(amount) as total_amount,
    AVG(amount) as average_fine,
    SUM(CASE WHEN notified THEN 1 ELSE 0 END) as notified_count
FROM fines 
WHERE DATE(created_at) = CURRENT_DATE;

## 🚢 Деплой:
Вариант 1: Локальный сервер:
# Установка как systemd сервис
sudo cp wb-bot.service /etc/systemd/system/
sudo systemctl enable wb-bot
sudo systemctl start wb-bot

Вариант 2: Docker:
# Dockerfile
FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt
COPY . .
CMD ["python", "bot/main.py"]

Вариант 3: Облачный хостинг:
AWS: EC2 + RDS PostgreSQL
Google Cloud: Compute Engine + Cloud SQL
Heroku: Procfile + Heroku PostgreSQL

Рекомендации по деплою
Используйте отдельного пользователя для бота
Настройте автоматические бэкапы БД
Настройте мониторинг uptime
Используйте reverse proxy (nginx) для мок-сервера
Настройте ротацию логов

## 🔒 Безопасность:
Рекомендации
Хранение секретов: только в переменных окружения
Доступ к БД: минимальные необходимые права
API ключи: регулярная ротация
Логи: без чувствительной информации
Обновления: регулярное обновление зависимостей

Безопасность Telegram бота
Не используйте бота в публичных каналах
Ограничьте список разрешенных пользователей
Используйте webhook вместо polling в продакшене

## 📈 Масштабирование:
Для увеличения нагрузки
Оптимизация БД: индексы, партицирование
Кэширование: Redis для частых запросов
Очереди задач: Celery для асинхронной обработки
Балансировка: несколько инстансов бота

## 📜 Лицензия
Creative Commons Attribution-NonCommercial 4.0 International (CC BY-NC 4.0)
Этот проект распространяется под лицензией Creative Commons Attribution-NonCommercial 4.0 
International (CC BY-NC 4.0).

Вы можете:
Делиться — копировать и распространять материал на любом носителе и в любом формате
Адаптировать — делать ремиксы, преобразовывать и брать за основу материал

При следующих условиях:
Атрибуция — Вы должны указать авторство, предоставить ссылку на лицензию и указать, 
были ли внесены изменения
Некоммерческое использование — Вы не можете использовать материал в коммерческих целях
Пример указания авторства:
WB Fines Monitor Bot
Автор: [Надежда Байкова]
Лицензия: CC BY-NC 4.0
Ссылка на проект: [https://github.com/BaikNadia/wb_bot]

⭐ Если этот проект был полезен для вас, поставьте звезду на GitHub!

